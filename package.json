{
  "name": "sdgp",
  "version": "1.0.0",
  "description": "Systems development :D",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "postinstall": "electron-rebuild -f -w better-sqlite3",
    "lint": "node --check main.js && node --check src/js/preload.js && node --check src/js/renderer.js && node --check src/js/sidenav.js && node --check src/js/upload_handler.js",
    "test": "node --test",
    "ci": "npm run lint && npm run test",
    "build": "electron-builder --win --publish never"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JoeCrebbin/SDGP.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/JoeCrebbin/SDGP/issues"
  },
  "homepage": "https://github.com/JoeCrebbin/SDGP#readme",
  "devDependencies": {
    "@electron/rebuild": "^4.0.1",
    "electron": "^40.0.0",
    "electron-builder": "^26.8.1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.6.2"
  },
  "build": {
    "appId": "com.sdgp.app",
    "productName": "SDGP",
    "files": [
      "main.js",
      "src/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false
    }
  }
}
